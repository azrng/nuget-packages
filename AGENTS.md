---
rule_id: agents-root
version: 1.14.0
last_updated: 2026-09-18
dependencies: []
---

# AGENTS.md

## 目标
- `AGENTS.md`：只保留全局协作规范、阶段门控、任务机制、交付要求与子规则导航
- `backend-AGENTS.md`：接口、服务、数据访问、缓存、异常处理与后端测试
- `doc-AGENTS.md`：设计文档、契约说明
- `infra-AGENTS.md`：端口、环境变量、Docker、启动与发布链路验证
- `CLAUDE.md` / `GEMINI.md`：入口提示文件，只提醒先读 `AGENTS.md`
- `TASK.md`：唯一活动任务记录文件

---

## 最小充分上下文
- 简单问题（纯问答、单文件、不影响业务逻辑、不新增依赖）与单点低风险改动（单文件、不影响核心链路、不涉及权限 / 缓存 / 迁移 / 发布）：只读命中的子规则和目标文件，不默认预读全部子规则
- 只有在需要实际落文件、任务跨服务端实现 / 文档 / 发布链路、涉及数据库、缓存、鉴权、发布、环境变量等高风险内容、或当前信息不足以判断影响范围时，才继续补读更多规则或实现文件
- 任务升级时按命中场景逐步补充上下文，而不是一开始加载全部信息

---

## 根文件原则
- 根 `AGENTS.md` 只放所有任务都需要知道的规则，不重复展开后端、文档、基础设施细节
- 用户指令始终高于本文件
- 先理解业务目标与现有实现，再修改代码或规则
- 优先复用现有结构，避免无关重构和过度设计
- 代码先服务于人，再服务于机器：方案先按 KISS 收敛，核心逻辑优先直白写法，复杂分支用行内注释说明"为什么"
- 优先做可验证的改动；无法验证时必须说明原因、影响和建议下一步

---

## 子规则索引

| 文件 | 适用范围 | 何时阅读 |
| --- | --- | --- |
| `backend-AGENTS.md` | 服务端逻辑、数据访问、缓存、异常处理、服务端测试 | 涉及接口、Service、Repository、脚本或数据链路时 |
| `doc-AGENTS.md` | 设计文档、契约说明 | 涉及阶段 0、文档维护或契约说明时 |
| `infra-AGENTS.md` | Docker、端口、环境变量、启动与发布链路 | 涉及部署、容器化、环境变量或交付链路时 |

- 发布或高风险改动：至少同时读当前文件和所有命中的子规则
- 小规模规则修订或文案调整：命中后只读对应子规则的相关段落，不必通读全文

### 按需阅读顺序
先读本文件确认全局规则与任务阶段 → 按任务类型命中对应子规则 → 再读相关模块与现有实现，确认复用点后动手。

---

## 开发阶段

### 三个阶段

```text
阶段 0          阶段 1                阶段 2
设计文档   →   服务端与数据实现   →   验证与交付
（用户确认）    （后端实现角色）        （交付验证角色）
```

- 新功能需经过完整阶段流程，以下场景可简化或跳过阶段 0：
  - Bug 修复
  - 轻量配置、环境变量或部署说明调整
  - 单个字段增删改，且不引入新业务流程
- 除上述场景外，各阶段必须满足入场条件后方可进入下一阶段
- 阶段括号仅表示推荐职责角色，不绑定具体 AI 工具；单 Agent 场景下可在满足门控条件后连续执行各阶段

### 阶段职责
- 阶段 0：需求整理、设计文档、契约说明，细则见 `doc-AGENTS.md`
- 阶段 1：服务端逻辑、数据访问、权限控制与测试，细则见 `backend-AGENTS.md`
- 阶段 2：构建、启动、发布与交付验证，结合当前文件与 `infra-AGENTS.md`

---

## 全局工作规则
- 先理解，再修改；不要基于猜测改代码
- 优先通过日志、数据、断点、测试等证据定位问题
- 先复用，再新增；不新增平行配置体系，不绕过现有架构
- 涉及新增、修改、删除等业务逻辑时，必须能真实生效并持久化
- 输出保持简洁，但分析、实现、验证要完整
- 已确认无变更风险的文件无需重复阅读；出现上下文缺口时必须补读
- 删除文件、删除目录、批量重构、数据破坏性变更、环境清理等高风险操作，必须先获得明确确认

### 查证优先级
不确定 API、依赖版本、文件是否存在、业务规则、配置项或命令结果时，按以下顺序查证：
1. 先检索项目代码、配置和测试。
2. 再读取相关实现文件、文档、任务记录和开发记录。
3. 再查看项目锁定的依赖版本、包配置或官方文档。
4. 仍无法确认时，列为待确认项并询问用户；不得把假设写成事实。

### 纠错与回退机制
- 阶段 0 输出的接口契约、DTO 或字段说明在阶段 1 发现缺口时，应先回到契约变更流程，修改 DTO / 接口定义后同步 AppService、数据访问映射、测试和相关文档，再继续实现；不得绕过契约直接补临时字段
- 发现前一阶段产物与需求、设计文档或真实实现冲突时，先说明冲突、影响范围和建议回退点，再按最小影响范围修正
- 自动提交后发现问题时，优先追加修复提交；只有用户明确要求且确认影响范围后，才执行回滚类 Git 操作
- 涉及数据库、迁移、发布产物、容器或环境清理的回退，必须先确认备份、回滚窗口和可验证恢复方式
- 纠错过程需要更新 `TASK.md` 的状态；阻塞项或交接说明按本文件「任务管理」的范围约定处理

### 实施约束
- 修改保持最小扰动，不做无关格式化、无关重构、无关依赖升级或大面积重排。
- 新增功能或较大改动前，先检索相似实现、调用方和测试，确认复用点、影响范围与验证方式。
- 修复问题必须定位根因并验证原始复现场景，不得通过吞异常、绕过校验或无依据的空判断掩盖问题。
- 涉及跨层职责、数据流、权限、缓存、状态流转或持久化时，先确认架构边界，避免把职责放到错误层级。
- 发现更简单实现、需求缺口、验证缺口或潜在风险时，主动提出最小建议；未经确认不扩大实现范围。

---
---

## 编码与文档规则
- 所有源码、配置、文档统一使用 `UTF-8`
- 读取或修改含中文文件时，如出现乱码，先判断是显示问题还是文件损坏，未确认前禁止覆盖
- Windows / PowerShell 下读取中文文件必须显式使用 `UTF-8`
- Windows / PowerShell 下处理中文 Git 提交信息时，优先使用 `git commit -m` / 多个 `-m` 参数，或使用 `UTF-8` 编码文件配合 `git commit -F <file>`
- 禁止通过标准输入管道直接传递中文提交信息，如 `git commit -F -`、here-string 管道等，避免编码链路把中文写成 `?`
- 禁止使用可能隐式改变编码的方式直接改写源码文件，如 shell 重定向、`Out-File`、`Set-Content`
- 修改含中文内容后，必须重新读取确认关键中文显示正常

---

## 任务管理
- `TASK.md` 是唯一活动任务记录文件，只维护最小任务记录与当前仍需关注的任务
- 开始工作前先检查是否已有任务；已有则更新原任务，没有则新增
- 只读评审、纯问答、现状分析且不落文件时，可不更新 `TASK.md` 和提交记录；若分析后转为规则或代码修改，则按正常任务流程补齐
- 任务状态仅使用：`TODO`、`DOING`、`BLOCKED`、`REVIEW`、`DONE`

### 任务状态触发条件
- `TODO`：目标已明确，尚未开始
- `DOING`：已开始分析、修改或验证
- `BLOCKED`：遇到技术障碍、依赖缺失或需用户决策，无法继续推进；更新时写明原因与所需支持
- `REVIEW`：已完成实现与最小自查，等待用户确认或代码审查；仅用于用户明确要求评审或跨 Agent / 跨会话交接的场景，不适用自动提交
- `DONE`：验证完成、结论明确且无需继续跟进；单 Agent 会话完成实现与验证后可直接置为 `DONE`，随后按「提交触发规则」处理
- 每条任务至少包含：任务 ID、任务名称、任务目标、当前阶段、负责人 AI、任务状态、优先级、最近更新时间
- 「最近完成」归档区的 `DONE` 任务仅保留：任务 ID、任务名称、状态、最近更新时间
- 过程叙述（已完成步骤、待用户确认、阻塞项、下一步）一律写在最终对话输出，不写入 `TASK.md`；仅跨 Agent / 跨会话交接（避免后续 Agent 重新推断上下文）或出现需用户决策的阻塞时才写入

### 任务清理规则
- `TASK.md` 只保留当前活跃任务和最近 5 条 `DONE` 任务
- `DONE` 任务超过 5 条时，自动删除最早的已完成任务
- `BLOCKED`、`REVIEW` 任务不得删除，避免仍需处理的事项从主表消失

### 文档同步要求
- 命令、脚本、环境变量、启动方式、数据结构或对外接口约定发生变化时，必须同步更新相关文档；同步范围与目录细则见 `doc-AGENTS.md`「文档更新要求」

### NuGet 包变更记录
- NuGet 包目录下的 `README.md` 只保留项目定位、安装方式、主要用法和当前版本的简要变更；当版本历史持续增长、明显影响使用说明阅读时，将历史记录迁移到同目录的 `CHANGELOG.md`。
- `CHANGELOG.md` 按版本号倒序维护，每个版本独立成节；迁移既有历史时保留原有条目，不因文档拆分改写发布事实。
- README 中指向变更记录的链接使用绝对 GitHub HTTPS 地址，确保 NuGet 页面渲染后可以直接访问仓库中的 `CHANGELOG.md`。
- 保持项目文件的 `<PackageReadmeFile>` 指向 `README.md`；`PackageProjectUrl` 继续指向项目文档入口，`RepositoryUrl` 继续指向代码仓库，不用变更记录替代 NuGet 首页说明或项目主页。
- 拆分或新增版本记录后，至少核对 README 链接、CHANGELOG 最新版本、README 安装示例与 csproj `<Version>` 一致；文档改动按文档规则重新读取并检查 UTF-8，涉及打包链路时补一次 `dotnet pack` 等价验证。

---

## Git 与交付

### Commit 规范
- 提交信息优先采用 Conventional Commits
- Commit 标题格式：`<type>(<scope>): <中文简述>`
- Windows / PowerShell 下提交前，优先确保当前终端与 Git 日志输出使用 `UTF-8`；若刚执行过中文提交，需回读最近一次提交信息确认标题与正文未乱码
- Commit 正文默认使用中文；非微小调整应补充以下字段：
  - `任务`
  - `阶段`
  - `原因`
  - `变更`
  - `风险`
  - `关联`
  - `AI`
- Commit 正文中的 `风险` 仅记录真实的代码、业务、数据、兼容性、部署或发布风险，不写过程性说明
- 未执行人工测试、未补 smoke test、未覆盖某项校验、仅做等价验证等过程性说明，统一写在最终输出，不写入 Commit 正文
- 提交前确认 `TASK.md` 已同步；若未同步，需说明原因
- push、PR / MR、交付说明应优先复用最近一次 Commit 正文中的结构化字段，并使用中文摘要

### 提交触发规则
- 当用户请求“开始开发”而非“只出方案”时，若本次代码、配置或规则改动已完成，且已完成本次改动所需验证，并已将 `TASK.md` 中对应任务状态更新为 `DONE`，则默认自动执行一次提交
- 自动提交前必须确认提交信息符合本节 `Commit 规范`
- 若验证尚未完成、任务状态尚未更新、仍存在明确阻塞，或用户明确要求暂不提交，则不得自动提交；需先说明原因与当前状态

### 交付与验证
- 用户请求“开始开发”（而非“只出方案”）且实现完成时，默认继续做交付收口，完成后按「提交触发规则」自动提交
- 按改动类型分层验证：
  - 文档或规则文件：重新读取改动文件，确认 `UTF-8` 与关键中文正常，无需强制 `lint` / `build`
  - 后端 / 数据访问 / 缓存 / 迁移：按 `backend-AGENTS.md`「提交前最小回归」执行
  - 环境变量 / Docker / 启动 / 发布链路：按 `infra-AGENTS.md`「提交前最小回归」执行
  - 启动链路改动：补构建与运行验证
  - 核心逻辑改动：补关键接口、服务或数据链路验证
  - 高风险改动：至少补一次应用级、接口级或脚本级 smoke test
  - 环境变量新增项：同步到 `.env.example` 或对应配置示例
  - 文档、`TASK.md`、忽略文件按需同步
- 仓库缺少对应脚本时，可用“等价验证”替代，但必须在最终说明中写清执行内容、未覆盖校验和后续建议
- 若某项验证未执行，必须明确说明未执行项、原因、影响范围和建议下一步
- 最终输出优先使用中文，并至少说明改动内容、修改文件、已执行和未执行的校验、当前任务状态及风险、阻塞或假设。

---

## 重复错误回顾机制
- 同一类问题因规则缺口在同一任务中连续两次阻塞交付，或跨任务重复出现时，必须做一次简短回顾
- 回顾内容保持简短，至少包含：错误现象、触发场景、根因、本次补充或修改的规则、后续如何避免复发
- 回顾结论必须落实到当前架构目录的 `AGENTS.md` 或对应子规则文件中

---
