# 清理根目录误生成依赖包

## 本次目标
- 排查并删除误生成到仓库根目录的 NuGet 依赖包缓存目录。

## 核心改动
- 核实根目录下 60 个目录为 NuGet 包缓存结构，约 225 MB。
- 定向删除这批误生成的包目录，保留 `src/`、`test/`、`doc/` 等项目内容不变。
- 删除后复核根目录，确认上述包目录已清空。

## 修改文件
- `TASK.md`
- `doc/devlog/2026-04-15-14-20-12-清理根目录误生成依赖包.md`

## 校验情况
- 已按目录名白名单执行删除，并在删除前校验目标路径均位于仓库根目录内。
- 已复查根目录，确认 60 个包缓存目录已不存在。
- 已通过 `git status --short` 复核，确认对应未跟踪包目录不再出现。

## 风险或遗留项
- 根因表现为 NuGet 依赖缓存被写入仓库根目录；若后续仍把 `NUGET_PACKAGES` 指向当前仓库，包目录会再次生成。
- 本次未修改现有业务代码，也未补充 `.gitignore` 规则，避免用忽略规则掩盖错误的缓存路径配置。
