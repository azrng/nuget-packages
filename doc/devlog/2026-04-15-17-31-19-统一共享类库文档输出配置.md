# 统一 Shared 类库 XML 文档输出配置

## 本次目标
- 批量补齐 `src/Shared` 下类库项目的 XML 文档生成配置。
- 统一 XML 文档输出为按目标框架区分的文件名，避免并行构建写入冲突。

## 核心改动
- 统一将 `src/Shared` 下类库项目的 `DocumentationFile` 配置为 `$(OutputPath)$(AssemblyName).$(TargetFramework).xml`。
- 对缺失 `GenerateDocumentationFile` 的项目补充 `true` 配置。
- 将 `Common` 等项目中原本关闭或使用旧文件名格式的 XML 文档配置一并修正。
- 本次共更新 `57` 个 `csproj` 文件。

## 修改文件
- `TASK.md`
- `src/Shared/**/*.csproj`
- `doc/devlog/2026-04-15-17-31-19-统一共享类库文档输出配置.md`

## 校验情况
- 已执行扫描校验，确认 `src/Shared` 下所有 `csproj` 都存在且仅存在一条：
  - `GenerateDocumentationFile=true`
  - `DocumentationFile=$(OutputPath)$(AssemblyName).$(TargetFramework).xml`
- 已执行：`dotnet build PackPackages.slnx -c Release -p:GenerateDocumentationFile=true`
- 构建结果：成功，`0` 错误，存在大量仓库历史警告，本次未处理。

## 风险或遗留项
- 本次仅统一 XML 文档生成配置，没有处理构建过程中暴露出的历史警告和依赖漏洞告警。
- 部分项目因原始文件排版差异，属性块出现了额外空行，但不影响构建和打包行为。
